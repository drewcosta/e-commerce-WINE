{
  "version": 2,
  "builds": [
    { "src": "api", "use": "@vercel/node" },
    { "src": "wine", "use": "@vercel/static-build" }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "api/$1" },
    { "src": "/(.*)", "dest": "wine/$1" }
  ]
}
